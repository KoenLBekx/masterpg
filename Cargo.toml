[package]
name = "masterpg"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
string_io_and_mock = { version = "1.0.0", path = "../string_io_and_mock" }
nom = { version = "7.1.3", features = ["alloc"] }
tree_by_path = { version = "0.1.0", path = "../tree_by_path" }
